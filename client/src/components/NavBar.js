import React, {Component} from "react";
import './../App.css';

class NavBar extends Component {
    render() {
        return (
            <div>
                This is a bar that goes at the top
            </div>
        );
    }
}

export default NavBar;